<?xml version="1.0" encoding="UTF-8" ?>
<site>
	{% set share_list = model.get('/test/_design/share-view/_view/share-list?include_docs=true') %}

	<page>
		<url>/</url>
		<context>{{ share_list }}</context>
		<template>home.html</template>
	</page>

	{% for row in share_list.rows %}
	<page>
		<url>/share/{{ row.id }}/</url>
		<context>
			{{
				model.get(
					api = '/test/_design/share-view/_view/share?keys=["' + row.id +'"]&include_docs=true',
					immutable = True
				)
			}}
		</context>
		<template>document.html</template>
	</page>
	{% endfor %}
</site>
